import type { ConcourseAmenitySpec, StationLevelId } from '../../../data/stationCirculation';
export function ConcourseAmenities({ items, yFor }: { items: ConcourseAmenitySpec[]; yFor: (l: StationLevelId) => number }) { return <>{items.map((a, i) => <group key={`${a.kind}-${i}`} position={[a.x, yFor(a.level), a.z]} rotation={[0, a.yaw, 0]}><mesh position={[0, .9, 0]}><boxGeometry args={[a.width ?? 1.2, 1.8, .65]} /><meshStandardMaterial color={a.kind === 'ticketOffice' ? '#d8c7a5' : '#e9ecec'} /></mesh><mesh position={[0, 1.45, -.34]}><boxGeometry args={[(a.width ?? 1.2) * .8, .36, .03]} /><meshBasicMaterial color="#80c241" /></mesh></group>)}</> }

# Compteur « voyageurs en ligne » ET salons multijoueur (facultatifs).
#
# Ces deux mêmes valeurs activent DEUX choses, et pas une de plus n'est
# nécessaire : le compteur de visiteurs du menu, et la possibilité de voyager à
# plusieurs dans la même rame (systems/net). Toujours aucune table, aucun SQL -
# on n'utilise que les canaux Realtime, en présence et en diffusion.
#
# Le site fonctionne parfaitement SANS ces valeurs : le compteur reste masqué,
# l'entrée « Voyager ensemble » du menu n'existe pas, et la bibliothèque
# Supabase n'est jamais téléchargée. Pour les activer :
#
#   1. Créer un projet gratuit sur https://supabase.com (aucune table ni SQL
#      nécessaires — on n'utilise que le canal Realtime « Presence »).
#   2. Dans le tableau de bord : Project Settings → API.
#   3. Copier « Project URL » et la clé « anon public » ci-dessous.
#
# La clé « anon » est PUBLIQUE par nature : elle finit dans le code livré au
# navigateur, c'est prévu ainsi. Ne jamais mettre ici la clé « service_role ».
#
# En local : copier ce fichier en « .env » (ignoré par git) et renseigner les
# valeurs. En production (GitHub Pages) : les définir dans le dépôt GitHub via
# Settings → Secrets and variables → Actions → onglet « Variables ».

VITE_SUPABASE_URL=https://votre-projet.supabase.co
VITE_SUPABASE_ANON_KEY=votre-cle-anon-publique
